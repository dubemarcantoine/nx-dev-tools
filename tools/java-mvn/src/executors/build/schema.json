{
  "$schema": "http://json-schema.org/schema",
  "version": 2,
  "title": "Build executor",
  "description": "",
  "type": "object",
  "properties": {
    "root": {
      "type": "string",
      "description": "Where the command will be executed"
    },
    "args": {
      "type": "array",
      "description": "Arguments passed to the executor command"
    }
  },
  "required": ["root"]
}
